#include "pch.h"

namespace toolsTab {
    extern bool toolsInit;
    extern int invisframes;
    extern bool frameadvance;
    extern bool nextframe;

    void init();
    void draw();
    void close();
}