#include "pch.h"

using namespace std;

namespace FPSMultiplier {
    extern float g_target_fps;
    extern bool g_fps_enabled;
    extern bool g_enabled;
    extern bool g_disable_render;
    extern float g_left_over;

    void Setup();
}