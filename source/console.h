#include <windows.h>
#include <iostream>
#include <string>

using namespace std;

namespace Console
{
	void Write(string text);
	void WriteLine(string text);
	string Input();
	void Clear();
	void Init();
}